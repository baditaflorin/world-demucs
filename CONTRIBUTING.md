# Contributing

Thanks for improving World-Demucs.

## Local setup

```bash
npm install
make install-hooks
make dev
```

Before opening a change, run:

```bash
make test
make build
make smoke
```

Use Conventional Commits for commit messages, such as `feat: add stem controls`.
